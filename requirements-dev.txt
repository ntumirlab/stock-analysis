# CI 單元測試用的最小依賴（tests/unit 不需要 finlab / ta-lib）
pytest
PyYAML
python-dotenv  # ConfigLoader 依賴（tests/unit/test_config_loader.py）
numpy==1.26.4  # 與 environment.yml 對齊，避免 CI 解出 numpy 2.x 與容器不一致
pandas==2.2.3  # 與 environment.yml 的容器版本對齊（finlab 2.x 要求 pandas>=2.1）
